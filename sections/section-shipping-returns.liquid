<section class="container mx-auto">
  <div class="flex flex-col items-center justify-center">
    <h1 class="text-center text-4xl font-bold">{{ page.title }}</h1>
    <div class="prose prose-base text-center">{{ page.content }}</div>
  </div>
</section>
